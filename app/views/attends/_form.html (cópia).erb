<h3>Dia <%=l dia_selected, format: :long %></h3>
<h4><%= myclinic %></h4>


<%
commentario = "f.association :clinic, :label => t('clinic'), label_method: :fantasy_name, value_method: :id, prompt: t('select') "
comentario2 = "f.input :schedule, :label => t('schedule'), :as => :date_picker "
%>

<div class="row">
  <div class="col-lg-4">
      <div id="events_calendar">
        <%= render partial: "calendar", locals: {attends: @attends_todos} %>
      </div><!-- /events_calendar -->
  </div><!-- /col -->
  <div class="col-lg-4">
    <div id="lista-horarios">
    <%= render partial: "listagem", locals: {attends: @attends} %>
    </div><!-- /lista-horarios -->
  </div><!-- /col -->
  <div class="col-lg-4">
    <div id="cliente-atendimento">
         <%= simple_form_for(@attend) do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
     
    <% comentario4 = "f.hidden_field :clinic_id, value: my_clinic_id " %>
    <% comentario3 = "f.hidden_field :schedule, value: dia_selected" %>

    <% comentario5 = "f.input :schedule, :as => :hidden, :input_html => { :value => dia_selected } " %>

    <%= f.input :patient_id, :label => t('patient') %>
    <%= f.input :schedule, :label => t('schedule'), :as => :date_picker %>
    <%= f.input :appointment, :label => t('appointment'),:as => :time_picker %>
    <%= f.association :exam, :label => t('exams'), as: :check_boxes %>
    <%= f.association :requester, :label => t('requester'), prompt: t('select') %>
    <%= f.input :payment_detail, :label => t('payment_detail') %>
    <%= f.input :finalized, :label => t('finalized') %>
    <%= f.input :missed, :label => t('missed') %>
    <%= f.input :notes, :label => t('notes') %>
  </div>
<div class="actions text-right">
      <%= button_tag(type: 'submit', class: "btn btn-primary") do %>
        <span class="glyphicon glyphicon-floppy-disk"></span> Salvar
      <% end %>
  </div>
<% end %>
     


    </div><!-- /cadastra-ainda-nao-ta-usando -->
  </div><!-- /col -->  
</div><!-- /row -->